export enum COLORS {
    WHITE = 0xFFFFFF,
    BLACK = 0x000000,
    RED = 0xFF0000,
    GREEN = 0x00FF00,
    BLUE = 0x0000FF,
    YELLOW = 0xFFFF00
}

export let WIDTH: number = window.innerWidth;
export let HEIGHT: number = window.innerHeight;